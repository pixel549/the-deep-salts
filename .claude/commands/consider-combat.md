---
description: Decide whether a fight should happen, and set it up if so
---

Follow `dm-only/protocols.md` §4 — CONSIDERING COMBAT, exactly and in order.
Ask whether combat should happen at all before assuming it does. If it
proceeds: pull the actual statblock (build it properly if it doesn't exist),
then build `dm-only/combat-state.json` from it via `tools/combat_state/`
before the first action — never from the resolver's generic fallback
thresholds.

$ARGUMENTS
