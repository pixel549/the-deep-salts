---
description: Start a new Salts instance/run — roll parameters, build the plan before narrating
---

Follow `dm-only/protocols.md` §2 — INSTANCE START, exactly and in order. Build
the plan in `dm-only/` before narrating the wake-in — do not narrate first
and backfill the plan.

$ARGUMENTS
